import Controller from '@ember/controller';

export default Controller.extend({
	
	// lists: ['To Do', 'Projects', 'Groceries'],
	// actions: {
	// 	addList: function() {
	// 		let listName = this.get('listName')
	// 		this.get('lists').pushObject(listName)
	// 	}
	// }
});
