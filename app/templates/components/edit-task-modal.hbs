{{#if enabled}}
  <div class="modal-fog">
    <div class="modal-frame">
      <div class="modal-title">
        {{title}}
      </div>
      <div class="modal-body">
  			<button {{action 'toggleModal' task.id}}> x </button>

		    <div class="form-group">

	  		{{!-- <div class="description"> --}}
    		<span>Description:</span>
    		{{!-- {{task.description}} --}}
		      	
		  			{{!-- {{input value=task.description id=task.id}} --}}
		      	
		  			{{!-- option to delete task --}}
		  			<button {{action 'deleteTask'}}>Delete</button>

  			</div>
        {{yield}}
      </div>
    	
    </div>
  </div>
{{/if}}



